{
  "name": "@agrishield/backend",
  "version": "0.1.0",
  "private": true,
  "description": "AgriShield backend: read-through policy and settlement API plus the settlement keeper",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AgroShield/AgriShield-Networks"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^17.1.0",
    "fastify": "^5.12.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
